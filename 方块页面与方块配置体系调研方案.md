# 方块页面与方块配置体系调研方案

## 1. 现状结论

- 你现在看到的“杂项/直升机/坦克/飞机/vehicle 页面”本质是 5 个 `CreativeTabs`，在启动时硬编码创建。
- 代码里当前只有一个方块：`drafting_table`（及 lit 变体），并且直接注册到“杂项页”。
- 目前项目里没有“方块配置目录/方块信息管理器”，也没有通用的 `blocks/*.txt` 加载链路。

关键代码：

- `MCH_MOD.PreInit()` 创建页面：`creativeTabs/creativeTabsHeli/creativeTabsPlane/creativeTabsTank/creativeTabsVehicle`
- `MCH_MOD.PreInit()` 注册方块：`blockDraftingTable`、`blockDraftingTableLit`
- `MCH_CreativeTabs` 为自定义页签实现
- `MCH_InfoManagerBase` 是项目现有配置加载基类（`key=value`）

## 2. 能否新增“方块页面”

可以，且实现成本较低。

最小改动路线：

1. 在 `MCH_MOD` 增加 `public static MCH_CreativeTabs creativeTabsBlock;`
2. `PreInit()` 里创建 `creativeTabsBlock = new MCH_CreativeTabs("MCH-Reforged Blocks");`
3. `postInit()` 里增加 `creativeTabsBlock.setFixedIconItem(...)`
4. 给配置 `MCH_Config` 增加 `CreativeTabIconBlock`（可选，不加也能用轮播图标）
5. 新增方块注册时 `setCreativeTab(creativeTabsBlock)`，与现有 5 页并行

说明：

- `MCH_CreativeTabs.getTranslatedTabLabel()` 目前固定返回 `"MCH-Reforged"`，所以每页名称显示一样、靠图标区分。这是现有行为，不是 bug。
- 如需显示“Blocks/方块”页签名，可把 `MCH_CreativeTabs` 改成按构造参数返回不同 label（可作为后续增强）。

## 3. 方块配置体系是否可做

可以，建议完全复用你现有单兵配置化模式：

- 新增目录：`assets/mcheli/blocks/*.txt`
- 新增信息类：`MCH_BlockInfo extends MCH_BaseInfo`
- 新增管理器：`MCH_BlockInfoManager`（仿 `MCH_LightWeaponInfoManager`）
- `MCH_MOD.PreInit()` 增加 `MCH_BlockInfoManager.load(...)`
- `MCH_MOD` 增加 `registerBlocksFromConfig()` 动态注册方块与对应 `ItemBlock`
- 配方继续走 `MCH_ItemRecipe.addRecipe(...)`，语法与当前 `AddRecipe` 一致，降低学习成本

## 4. 推荐的 `blocks/*.txt` 写法

建议字段（第一版够用）：

- `DisplayName`：默认显示名
- `AddDisplayName=lang,text`：多语言名
- `BlockID`：1.7.10 旧式 ID（与现有项目风格一致）
- `Material`：`iron/rock/wood/glass/...`
- `Hardness`：硬度
- `Resistance`：爆炸抗性
- `LightLevel`：0.0~1.0
- `StepSound`：`metal/stone/wood/glass/...`
- `CreativeTab`：`misc/heli/plane/tank/vehicle/block`（建议新增 `block`）
- `ModelName`：方块模型名（可选）
- `TextureName`：方块贴图名（可选）
- `HasTileEntity`：是否有 TileEntity（可选）
- `TileEntityClass`：TileEntity 类名（可选，建议白名单映射）
- `AddRecipe` / `AddShapelessRecipe`：沿用现有配方解析器

## 5. 配置示例

文件：`assets/mcheli/blocks/supply_crate.txt`

```txt
DisplayName=Supply Crate
AddDisplayName=zh_CN,补给箱方块
BlockID=3460
Material=iron
Hardness=3.0
Resistance=15.0
LightLevel=0.0
StepSound=metal
CreativeTab=block
TextureName=supply_crate
HasTileEntity=false
AddRecipe="III","ICI","III",I,minecraft:iron_ingot,C,minecraft:chest
```

## 6. 配方语法注意事项（沿用现有实现）

- `AddRecipe`（有序）示例：
  - `AddRecipe="ABC","DEF","GHI",A,minecraft:iron_ingot,B,minecraft:redstone`
- `AddShapelessRecipe`（无序）示例：
  - `AddShapelessRecipe=minecraft:iron_ingot,minecraft:chest`
- 项目已支持中文逗号容错是 `splitParam` 侧能力；但 `MCH_ItemRecipe` 自身用逗号分割，建议配置仍使用英文半角逗号，避免歧义。

## 7. 风险与边界

- 1.7.10 方块 ID 管理本身有历史包袱，建议避免与现有 `BlockID_DraftingTable*` 冲突。
- 若要支持“自定义 TileEntityClass 字符串反射”，需加白名单，避免配置注入任意类。
- `lit`/`unlit` 双方块切换（像 drafting table）不建议第一版就配置化，可先做单方块版本。

## 8. 建议落地顺序

1. 先加 `creativeTabsBlock` 页面（不动原有逻辑）。
2. 先支持“无 TileEntity 的静态方块配置”（最稳）。
3. 再补 TileEntity 白名单与自定义渲染挂接。
4. 最后再考虑类似 drafting table 的双态切换方块。

## 9. 阶段3方案：智能生成方块（随机多载具 + 装订炮手）

目标能力：

- 检测方块附近 `10m` 内无实体时，自动在方块上方生成载具。
- 一个方块可配置多种载具，触发时随机抽取其中一种生成。
- 可选同时生成并装订 gunner（挂载到载具）。
- 支持一次性生成或按时间间隔循环生成。
- 方块在休眠状态（一次性已完成或周期模式冷却中）切换到休眠贴图。
- 当配置为“阵营 gunner”且该阵营在本局不存在时，触发时自动创建该阵营。

实现建议：

- 采用 `Block + TileEntity` 结构。
- 仅服务端执行检测和生成（`!world.isRemote`），客户端只负责显示状态贴图。
- 所有运行时状态（是否已触发、冷却计时、最近生成实体 UUID、当前状态）写入 TileEntity NBT。
- 随机载具从配置列表中抽样，可加权重以控制概率。

## 10. 阶段3参数设计（建议）

### 10.1 触发与检测参数

- `EnableSpawner=true|false`
- `CheckRadius=10.0`
- `CheckIntervalTick=20`（每多少 tick 扫描一次）
- `DetectPlayers=true|false`
- `DetectMobs=true|false`
- `DetectAnimals=true|false`
- `DetectVehicles=true|false`
- `DetectGunners=true|false`
- `IgnoreCreativePlayer=true|false`
- `IgnoreSpectator=true|false`（如需兼容更高版本语义可保留）

### 10.2 生成模式参数

- `SpawnMode=once|interval`
- `CooldownTick=1200`（`interval` 模式有效）
- `SpawnYOffset=1.0`
- `SpawnYawMode=random|block_facing|fixed`
- `SpawnYaw=0.0`（`fixed` 模式有效）
- `OneTimeKeepSleepTexture=true|false`

### 10.3 多载具随机参数（核心）

- `VehiclePool=heli:ah-64|plane:f-15|tank:type90|vehicle:humvee`
- `VehicleWeight=5|3|2|4`（与池子一一对应，可选，不填则等概率）
- `SpawnVehicleCount=1`（建议固定 1，防止刷怪式过载）
- `VehicleExtraNbt=`（可选，后续扩展）

说明：

- `VehiclePool` 支持多个候选项，触发时从池中随机抽取 1 个。
- 如提供 `VehicleWeight`，按权重随机；否则等概率随机。
- 候选项格式建议：`type:name`，`type` 取值 `heli/plane/tank/vehicle`。

### 10.4 gunner 装订参数

- `SpawnGunner=true|false`
- `GunnerMode=none|normal|faction`
- `GunnerSeatIndex=0`（默认第一个可用座位）
- `GunnerTargetType=0|1`（沿用现有 gunner 目标类型定义）
- `GunnerYaw=0.0`
- `GunnerPitch=0.0`

当 `GunnerMode=faction` 时新增：

- `GunnerFactionId=blue_team`
- `GunnerFactionName=Blue Team`
- `AutoCreateFaction=true|false`
- `AutoCreateFactionColor=0x3A66FF`（可选）

### 10.5 状态贴图参数

- `TextureActive=spawner_active`
- `TextureSleep=spawner_sleep`
- `TextureError=spawner_error`（可选：生成失败时显示）
- `StateSyncTick=5`（状态同步频率，降低网络包）

## 11. 阵营自动创建规则（新增）

触发流程（`GunnerMode=faction` 且 `AutoCreateFaction=true`）：

1. 方块触发时先读取 `GunnerFactionId`。
2. 查询当前世界/服务器阵营管理器中是否存在该阵营。
3. 若不存在，则按配置自动创建阵营（`GunnerFactionName/Color`）。
4. 创建成功后再生成 gunner，并把 gunner 绑定到该阵营。
5. 若创建失败，方块本次触发进入失败冷却，并显示 `TextureError`（若配置）。

兼容建议：

- 阵营创建必须在服务端执行并持久化存档。
- 阵营 ID 建议全局唯一，推荐前缀：`mchr_<pack>_<name>`。
- 对“自动创建”增加开关，默认开启可提升开箱即用体验。

## 12. 阶段3配置示例

文件：`assets/mcheli/blocks/vehicle_spawner_alpha.txt`

```txt
; 方块默认显示名
DisplayName=Vehicle Spawner Alpha
; 中文显示名
AddDisplayName=zh_CN,载具生成方块-阿尔法
; 方块ID（避免冲突）
BlockID=3461
; 方块材质（影响挖掘与物理表现）
Material=iron
; 方块硬度
Hardness=4.0
; 方块爆炸抗性
Resistance=20.0
; 方块发光强度（0.0~1.0）
LightLevel=0.0
; 方块脚步声
StepSound=metal
; 创造页签
CreativeTab=block
; 默认贴图（可作为兜底）
TextureName=vehicle_spawner_active

; 是否启用智能生成逻辑
EnableSpawner=true
; 检测半径（米）
CheckRadius=10.0
; 检测间隔（tick）
CheckIntervalTick=20
; 检测玩家实体
DetectPlayers=true
; 检测怪物实体
DetectMobs=true
; 检测动物/中立生物实体
DetectAnimals=true
; 检测载具实体
DetectVehicles=true
; 检测gunner实体
DetectGunners=true
; 是否忽略创造模式玩家
IgnoreCreativePlayer=true
; 是否忽略旁观者（高版本兼容预留）
IgnoreSpectator=false

; 生成模式：once=一次性，interval=周期
SpawnMode=interval
; 周期模式冷却时间（tick）
CooldownTick=1200
; 生成Y轴偏移（相对方块顶面）
SpawnYOffset=1.0
; 生成朝向模式：random|block_facing|fixed
SpawnYawMode=random
; 固定朝向角度（仅fixed模式使用）
SpawnYaw=0.0
; 一次性触发后是否保持休眠贴图
OneTimeKeepSleepTexture=true

; 候选载具池（触发时随机抽取一个）
VehiclePool=heli:ah-64|plane:f-15|tank:type90|vehicle:humvee
; 候选载具权重（与VehiclePool一一对应）
VehicleWeight=5|3|2|4
; 单次触发生成载具数量（建议1）
SpawnVehicleCount=1
; 载具额外NBT（预留字段）
VehicleExtraNbt=

; 是否生成并装订gunner
SpawnGunner=true
; gunner模式：none|normal|faction
GunnerMode=faction
; gunner座位索引（0=自动，1开始对应座位）
GunnerSeatIndex=0
; gunner目标类型（0友好/1阵营/2反导/3敌对）
GunnerTargetType=1
; gunner初始水平朝向
GunnerYaw=0.0
; gunner初始俯仰角
GunnerPitch=0.0

; 阵营ID（faction模式必填）
GunnerFactionId=mchr_blue_team
; 阵营显示名（创建阵营时使用）
GunnerFactionName=Blue Team
; 阵营不存在时是否自动创建
AutoCreateFaction=true
; 自动创建阵营的颜色（预留）
AutoCreateFactionColor=0x3A66FF

; 激活状态贴图
TextureActive=vehicle_spawner_active
; 休眠状态贴图（一次性完成或冷却中）
TextureSleep=vehicle_spawner_sleep
; 错误状态贴图（生成失败时）
TextureError=vehicle_spawner_error
; 状态同步间隔（tick）
StateSyncTick=5
```
