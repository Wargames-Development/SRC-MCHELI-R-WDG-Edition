diff --git a/src/main/java/mcs/vehicle/MCSTrackedSuspensionSolver.java b/src/main/java/mcs/vehicle/MCSTrackedSuspensionSolver.java
index 2b543c5..0330d90 100644
--- a/src/main/java/mcs/vehicle/MCSTrackedSuspensionSolver.java
+++ b/src/main/java/mcs/vehicle/MCSTrackedSuspensionSolver.java
@@ -766,6 +766,20 @@ final class MCSTrackedSuspensionSolver {
                 : scale(value, maximum / length);
     }
 
+    static double getHullLowerProbeHeight(
+            boolean front,
+            double configuredFrontHeight,
+            double configuredRearHeight,
+            double bottom,
+            double upper) {
+        double configuredHeight = front
+                ? configuredFrontHeight
+                : configuredRearHeight;
+        return isFinite(configuredHeight) && configuredHeight > 0.0D
+                ? clamp(configuredHeight, bottom, upper)
+                : bottom;
+    }
+
     static Vec3 calculateTemporalCollisionShift(
             Vec3 point, Vec3 hitPoint, Vec3 collisionNormal) {
         Vec3 movementAfterImpact = subtract(point, hitPoint);
@@ -1160,10 +1174,14 @@ final class MCSTrackedSuspensionSolver {
             double upper = Math.max(bottom, definition.collisionHeight - inset);
             double configuredFrontLowerHeight =
                     definition.hullFrontLowerProbeHeightMeters;
-            double frontLower = isFinite(configuredFrontLowerHeight)
-                    && configuredFrontLowerHeight > 0.0D
-                    ? clamp(configuredFrontLowerHeight, bottom, upper)
-                    : bottom;
+            double configuredRearLowerHeight =
+                    definition.hullRearLowerProbeHeightMeters;
+            double frontLower = getHullLowerProbeHeight(
+                    true, configuredFrontLowerHeight, configuredRearLowerHeight,
+                    bottom, upper);
+            double rearLower = getHullLowerProbeHeight(
+                    false, configuredFrontLowerHeight, configuredRearLowerHeight,
+                    bottom, upper);
             Vec3 totalShift = Vec3.createVectorHelper(0.0D, 0.0D, 0.0D);
             Vec3 velocity = linearVelocity;
 
@@ -1172,7 +1190,7 @@ final class MCSTrackedSuspensionSolver {
                     double x = xIndex == 0 ? -halfWidth : halfWidth;
                     for (int zIndex = 0; zIndex < 2; zIndex++) {
                         double y = yIndex == 0
-                                ? (zIndex == 1 ? frontLower : bottom)
+                                ? (zIndex == 1 ? frontLower : rearLower)
                                 : upper;
                         double z = zIndex == 0 ? -halfLength : halfLength;
                         Vec3 localPoint = Vec3.createVectorHelper(x, y, z);
diff --git a/src/main/java/mcs/vehicle/VehicleDefinition.java b/src/main/java/mcs/vehicle/VehicleDefinition.java
index 1ed6350..68df0cc 100644
--- a/src/main/java/mcs/vehicle/VehicleDefinition.java
+++ b/src/main/java/mcs/vehicle/VehicleDefinition.java
@@ -91,6 +91,7 @@ public class VehicleDefinition {
     public float suspensionAngularDampingPerSecond = 0.8F;
     public float hullProbeInsetMeters = 0.08F;
     public float hullFrontLowerProbeHeightMeters = 0.0F;
+    public float hullRearLowerProbeHeightMeters = 0.0F;
     public float suspensionVisualReferenceCompressionMeters = 0.0F;
     public SuspensionContact[] suspensionContacts = new SuspensionContact[0];
 
diff --git a/src/main/resources/assets/mcs/vehicles/m1a2.json b/src/main/resources/assets/mcs/vehicles/m1a2.json
index fe40749..f973dc8 100644
--- a/src/main/resources/assets/mcs/vehicles/m1a2.json
+++ b/src/main/resources/assets/mcs/vehicles/m1a2.json
@@ -35,14 +35,15 @@
   "suspensionTravelMeters": 0.32,
   "suspensionProbeRadiusMeters": 0.14,
   "trackDriveResponsePerSecond": 6.0,
-  "trackLateralResponsePerSecond": 1.0,
+  "trackLateralResponsePerSecond": 5.0,
   "trackLongitudinalGrip": 1.0,
-  "trackLateralGrip": 0.65,
+  "trackLateralGrip": 1.1,
   "trackTractionReachMeters": 0.55,
   "trackTractionRedistributionFraction": 0.75,
   "suspensionAngularDampingPerSecond": 0.8,
   "hullProbeInsetMeters": 0.08,
   "hullFrontLowerProbeHeightMeters": 1.05,
+  "hullRearLowerProbeHeightMeters": 1.05,
   "suspensionVisualReferenceCompressionMeters": 0.18,
   "debugRenderVehicleTuning": true,
   "debugRenderDamageHitboxes": false,
diff --git a/src/test/java/mcs/vehicle/MCSTrackedSuspensionContactPatchTest.java b/src/test/java/mcs/vehicle/MCSTrackedSuspensionContactPatchTest.java
index 012f3d0..5c878e4 100644
--- a/src/test/java/mcs/vehicle/MCSTrackedSuspensionContactPatchTest.java
+++ b/src/test/java/mcs/vehicle/MCSTrackedSuspensionContactPatchTest.java
@@ -69,6 +69,34 @@ public class MCSTrackedSuspensionContactPatchTest {
         }
     }
 
+    @Test
+    public void lowerHullProbeHeightsCanBeConfiguredIndependently() {
+        assertEquals(
+                1.05D,
+                MCSTrackedSuspensionSolver.getHullLowerProbeHeight(
+                        true, 1.05D, 0.90D, 0.08D, 2.32D),
+                EPSILON);
+        assertEquals(
+                0.90D,
+                MCSTrackedSuspensionSolver.getHullLowerProbeHeight(
+                        false, 1.05D, 0.90D, 0.08D, 2.32D),
+                EPSILON);
+    }
+
+    @Test
+    public void invalidLowerHullProbeHeightFallsBackToBottom() {
+        assertEquals(
+                0.08D,
+                MCSTrackedSuspensionSolver.getHullLowerProbeHeight(
+                        false, 1.05D, Double.NaN, 0.08D, 2.32D),
+                EPSILON);
+        assertEquals(
+                2.32D,
+                MCSTrackedSuspensionSolver.getHullLowerProbeHeight(
+                        false, 1.05D, 5.0D, 0.08D, 2.32D),
+                EPSILON);
+    }
+
     @Test
     public void temporalHullCorrectionPreservesUpwardAndSidewaysMovement() {
         Vec3 hitPoint = Vec3.createVectorHelper(0.0D, 1.1D, 0.2D);
diff --git a/src/test/java/mcs/vehicle/TrackedSuspensionDefinitionTest.java b/src/test/java/mcs/vehicle/TrackedSuspensionDefinitionTest.java
index 0d7e176..5772cf0 100644
--- a/src/test/java/mcs/vehicle/TrackedSuspensionDefinitionTest.java
+++ b/src/test/java/mcs/vehicle/TrackedSuspensionDefinitionTest.java
@@ -28,9 +28,9 @@ public class TrackedSuspensionDefinitionTest {
         assertEquals(0.32F, definition.suspensionTravelMeters, EPSILON);
         assertEquals(0.14F, definition.suspensionProbeRadiusMeters, EPSILON);
         assertEquals(6.0F, definition.trackDriveResponsePerSecond, EPSILON);
-        assertEquals(1.0F, definition.trackLateralResponsePerSecond, EPSILON);
+        assertEquals(5.0F, definition.trackLateralResponsePerSecond, EPSILON);
         assertEquals(1.0F, definition.trackLongitudinalGrip, EPSILON);
-        assertEquals(0.65F, definition.trackLateralGrip, EPSILON);
+        assertEquals(1.1F, definition.trackLateralGrip, EPSILON);
         assertEquals(0.55F, definition.getSafeTrackTractionReachMeters(), EPSILON);
         assertEquals(
                 0.75F,
@@ -39,10 +39,15 @@ public class TrackedSuspensionDefinitionTest {
         assertEquals(0.8F, definition.suspensionAngularDampingPerSecond, EPSILON);
         assertEquals(0.08F, definition.hullProbeInsetMeters, EPSILON);
         assertEquals(1.05F, definition.hullFrontLowerProbeHeightMeters, EPSILON);
+        assertEquals(1.05F, definition.hullRearLowerProbeHeightMeters, EPSILON);
         assertEquals(
                 0.0F,
                 new VehicleDefinition().hullFrontLowerProbeHeightMeters,
                 0.0F);
+        assertEquals(
+                0.0F,
+                new VehicleDefinition().hullRearLowerProbeHeightMeters,
+                0.0F);
         assertEquals(
                 0.18F,
                 definition.getSafeSuspensionVisualReferenceCompressionMeters(),
