# 单兵激光制导剥离落地前边界清单

## 1. 目的

- 在“单兵激光制导从 GPS 逻辑剥离”的改造前，先把所有高风险边界条件列清楚。
- 避免从“GPS 与单兵激光竞争”演变成“单兵激光与机载激光竞争”。

## 2. 设计约束（必须先定）

1. 单兵激光状态与机载激光状态必须物理隔离，不能共享同一全局对象。
2. 同一状态通道同一 tick 只能有一个写入源。
3. 服务端状态为权威，客户端状态只能用于渲染与本地提示。
4. 每条激光状态必须有可证明失活机制（显式失活 + 超时失活）。
5. GPS 状态仅服务 GPS 武器，不再承担激光状态。

## 3. 边界矩阵（先验证再编码）

### 3.1 持有者维度

- 玩家（徒步手持单兵武器）
- 玩家（驾驶载具）
- 玩家（载具座位）
- AI gunner（载具驾驶位）
- AI gunner（非驾驶位）

### 3.2 武器维度

- 单兵 TV 导弹激光子模式
- 单兵其他激光制导武器
- 机载 TV 导弹激光子模式
- 机载 GPS 导弹（AS/GPS）
- 非制导武器（对照组）

### 3.3 状态切换维度

- 开镜瞄准
- 发射瞬间
- 发射后保瞄窗口
- 空仓
- 换弹
- 切枪
- 上车 / 下车
- 死亡 / 重生
- 维度切换 / 断线重连

### 3.4 多人并发维度

- 两名玩家同时使用单兵激光
- 玩家单兵激光 + 玩家机载激光并行
- 玩家激光 + AI gunner 激光并行
- 同队与敌队混合并发

### 3.5 网络维度

- 正常延迟
- 高延迟（150~300ms）
- 短暂丢包
- 重传导致旧包晚到（乱序）

## 4. 关键冲突点与防护规则

## 4.1 冲突点

- 同一个 `ownerId` 在同一时段既有手持激光又有机载激光。
- 玩家快速切换“手持->上车->手持”导致旧状态残留。
- 客户端已失活但服务端未失活（或相反）导致导弹追踪与渲染不一致。
- 乱序网络包用旧坐标覆盖新坐标。

## 4.2 防护规则

1. **双通道键**：状态 key 使用 `ownerId + sourceType(handheld/aircraft)`。
2. **写入仲裁**：每通道仅接受各自固定写入端：
   - handheld 仅 `MCH_ClientLightWeaponTickHandler`
   - aircraft 仅机载武器 guidance 逻辑
3. **序列号机制**：包携带 `sequence`，服务端仅接受更大序列。
4. **TTL 超时**：超过 `N tick` 未更新自动失活。
5. **事件失活**：切枪/下车/死亡/断线即时失活。
6. **渲染只读对应通道**：单兵 HUD 只读 handheld，机载 HUD 只读 aircraft。

## 5. 落地前验收用例（最小集）

1. 单兵激光标点 60 秒持续稳定，无随机消失。
2. 单兵切枪后激光点立即失活，不污染 GPS。
3. 玩家上车后机载激光可用，且不继承手持旧点位。
4. 机载 GPS 武器显示 `GPSPosition.png + GPS`，单兵显示十字框 + `LZR`。
5. 两名玩家并发激光互不覆盖。
6. 玩家死亡/重生后旧激光点不残留。
7. 300ms 延迟下导弹追踪仍与最新点位一致。

## 6. 重新构思后的执行步骤

## 阶段A：边界先行（不改核心行为）

1. 定义 `LaserState` 数据结构与通道键。
2. 定义包协议字段（`sourceType/sequence/active/xyz`）。
3. 定义失活策略（显式 + TTL）和事件钩子清单。

交付物：
- 协议草案
- 状态机图
- 边界测试清单（本文件）

## 阶段B：并行引入 LaserState（保持旧链路可回退）

1. 保留现有 GPS 逻辑，新增 LaserState 写入但不替换导弹读取。
2. HUD/调试面板可同时观测 GPS 与 LaserState（便于比对）。
3. 跑边界矩阵，确认没有竞争和状态残留。

交付物：
- 可开关的双轨运行
- 观测日志与复现记录

## 阶段C：切换读取源（核心切换）

1. 单兵导弹与 TV 激光子模式导弹改读 LaserState。
2. 移除单兵路径对 `MCH_GPSPosition.set` 的依赖。
3. GPS 链路仅保留给 GPS 武器与 K 面板。

交付物：
- 单轨 LaserState 生效
- GPS 与 LZR 职责彻底分离

## 阶段D：收尾与回归

1. 删除过渡兼容代码与临时日志。
2. 全量回归（单机/局域网/专用服）。
3. 输出最终维护文档（状态图 + 数据流）。

## 7. 决策建议

- 当前最稳策略：先做阶段A与阶段B，再推进阶段C。
- 若直接一步切到阶段C，复现困难的偶发竞争问题会很难定位。
- 建议每阶段都保留可开关开关项，便于线上快速回退。

